<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

   <ListPreference
        android:key="@string/pref_key_movies_sort_order"
        android:title="@string/pref_title_movies_sort_order"
        android:defaultValue="-1"
        android:entries="@array/pref_titles_movies_sort_order"
        android:entryValues="@array/pref_values_movies_sort_order"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
